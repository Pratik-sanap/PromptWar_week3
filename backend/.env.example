# Gemini API Key (Required for live GenAI functionality)
GEMINI_API_KEY=your_gemini_api_key_here

# CORS — comma-separated origins allowed to call the API (security.md §4)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
